mage vars:create [options] <name>

Description:
    This command provides a way to create a new vars file for a magento env. The name for the new env is defined with <name> arg.

Options:
    --help
        Displays this document.
    
    --force
        Force file creation even if it already exists
